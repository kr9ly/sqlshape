package dto

import (
	"time"
)

// Blank has no imports at all: a fix that needs one (time, for created_at) must add a
// whole new import declaration after the package clause.
type Blank struct {
	ID        int64
	CreatedAt time.Time
}

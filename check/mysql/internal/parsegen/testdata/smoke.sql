SELECT 1
;;
SELECT a, b FROM t WHERE c = 1 AND d IN (SELECT e FROM u) ORDER BY a LIMIT 10
;;
INSERT INTO t (a, b) VALUES (1, "x"), (2, `y`)
;;
CREATE TABLE t (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(255) NOT NULL, created DATETIME DEFAULT CURRENT_TIMESTAMP) ENGINE=InnoDB
;;
SELECT /*+ NO_INDEX(t) */ * FROM t
;;
SELECT * FROM t WHERE json_col->>"$.a" = 1
;;
SELECT 1 +
;;
WITH RECURSIVE cte AS (SELECT 1 AS n UNION ALL SELECT n+1 FROM cte WHERE n < 10) SELECT * FROM cte
;;
UPDATE t SET a = a + 1 WHERE id = ?
;;
